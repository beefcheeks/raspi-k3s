namespace: kube-system

# Manifest-only: we no longer ship our own Traefik chart. The k3s-bundled Traefik (v3)
# is customized via the HelmChartConfig below; the middlewares are plain traefik.io CRs.
resources:
  - resources/helmchartconfig.yaml
  - resources/middleware.yaml
